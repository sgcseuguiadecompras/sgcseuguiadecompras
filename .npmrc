# Permitir scripts de build para pacotes que precisam de compilação nativa
package-scripts:enabled=true

# Aprovar pacotes específicos que requerem scripts de build
# sharp é usado pelo Next.js para otimização de imagens
onlyBuiltDependencies[]=sharp
